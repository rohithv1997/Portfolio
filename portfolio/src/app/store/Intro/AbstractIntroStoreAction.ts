import { AbstractStoreAction } from '../AbstractStoreAction';
import { IIntroState } from './IIntro.State';

export abstract class AbstractIntroStoreAction extends AbstractStoreAction<IIntroState> {}
